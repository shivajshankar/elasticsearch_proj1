name: Deploy to K3s (Search Namespace)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'production'
        type: choice
        options:
        - production
        - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_DATA }}" | base64 -d > ~/.kube/config
          kubectl config use-context default-context

      - name: Create search namespace if not exists
        run: |
          kubectl create namespace search --dry-run=client -o yaml | kubectl apply -f -

      - name: Create GitHub Actions RBAC for search namespace
        run: |
          cat <<EOF | kubectl apply -f -
          apiVersion: rbac.authorization.k8s.io/v1
          kind: Role
          metadata:
            name: github-actions-role
            namespace: search
          rules:
          - apiGroups: ["", "apps", "networking.k8s.io", "batch", "extensions"]
            resources: ["*"]
            verbs: ["*"]
          ---
          apiVersion: rbac.authorization.k8s.io/v1
          kind: RoleBinding
          metadata:
            name: github-actions-binding
            namespace: search
          subjects:
          - kind: ServiceAccount
            name: github-actions
            namespace: github-actions
          roleRef:
            kind: Role
            name: github-actions-role
            apiGroup: rbac.authorization.k8s.io
          EOF

      - name: Deploy Elasticsearch
        run: |
          kubectl apply -n search -f deploy/k8s/elasticsearch.yaml

      - name: Deploy Kibana
        run: |
          kubectl apply -n search -f deploy/k8s/kibana.yaml

      - name: Verify deployments
        run: |
          echo "Current resources in search namespace:"
          kubectl get all -n search
          echo -e "\nDeployment to ${{ github.event.inputs.environment }} environment completed"
